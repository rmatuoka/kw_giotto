<div id="prevvv" style="z-index: 999;position: absolute;display: block;float:left;background-image: url(../images/bt_prev_arr_g.png);top: 43%; height:75px;width:50px;cursor: pointer;"></div>
<div id="nexxxt" style="z-index: 999;position: relative;display: block;float:right;background-image: url(../images/bt_next_arr_g.png);top: 43%; height:75px;width:50px;cursor: pointer;"></div>
<%= image_tag ("bt_close_prod.png"), :class => "bt_close_prod"%>

<div class="content_area slideshow3">
<%$i = 0%>
<% @products.each do |product|%>
<% @product_images = product.product_images.all %>
<% if product.id == @product.id%>
<%$b = product.id%>
  <script>;
    item = <%= $i%>;
    bullet = <%= $b%>;
  </script>
<%end%>
    <div class="box_area">
        <div class="box_area_left">
            <div id="slideshowX<%=product.id%>" class="slideshow2" style="position: relative; width: 270px;display:block; height: 390px; overflow: hidden;">   	
                <% @product_images.each do |image| %>
                  <table height="390" style="border-style:none;display:table !important;">
                    <tr>
                      <td align="center" valign="middle" style="vertical-align:middle !important;">
                      <%=link_to(image_tag(image.image("rotative")), image.image.url, :class => "fancybox", :rel => product.code)%>
                      </td>
                    </tr>
                  </table>
                  
                <% end %>
            </div>
            <% if @product_images.count > 1%>
            <div id="bullets<%=product.id%>" class="bullets"></div>
            <%end%>
            <span class="text_vjaimg">Clique na imagem para ampliar</span> 
        </div>
        <div class="box_area_right">
            <p class="title_prod"><%= product.name%></p>
            <p class="text_prod"><%= product.description %></p>
            <p class="title_spec">Especificaçõs:</p>
            <p class="text_prod"><b>Código: </b><%= product.code %></p>
            <p class="text_prod"><b>Código EAN-13: </b><%= product.ean %></p>
            <p class="text_prod"><b>Código DUN-14: </b><%= product.dun %></p>
            <p class="text_prod"><b>Peso líq (Un): </b><%= product.weight %></p>
            <p class="text_prod"><b>Peso Total (g): </b><%= product.total_weight %></p>
            <p class="text_prod"><b>Cubagem (m³): </b><%= product.cubage %></p>
            <p class="text_prod"><b>Quantidade (volume): </b><%= product.amount %></p>
            <p class="text_prod"><b>Dimensões: </b><%= product.dimensions %></p>
            <p class="text_prod"><b>Cores: </b><%= product.color %></p>
        </div>
    </div>
    <script>
        $('#slideshowX'+<%=product.id%>).cycle({ 
          fx:     'scrollHorz', 
          slideResize: false,
          fit: 1, 
          timeout: 0,
          pager: '#bullets'+<%=product.id%>
          });
    </script>
    <%$i = $i+1%>
    
    <%end%>
</div>

