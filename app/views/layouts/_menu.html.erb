	<div class="header" id="header">
            <div class="logo left">
				<%= link_to image_tag("logo.png"), root_path %>         
            </div>     
            <!--Menu --> 
            <div class="menu_container right">    
                <ul class="menu">
                    <li><%= link_to t("about_title"), about_us_index_path%>                  
                    </li>
                    <li>
                    	<%= link_to t("show_products"), products_path%>
                        <ul class="sub_menu">
                        	<li><%= link_to t("show_release"), releases_path %></li>                        	
                            <% if @categories.count > 0 %>
                            <% @categories.each do |category| %>
                            <li><%= link_to category.title, product_path(category.id) %></li>
                            <% end %> 
                            <% end %> 
                            <li><%=link_to t("show_toys"), "http://www.miellebrinquedos.com.br/", :target => "_blank"%></li>                          
                        </ul>
                    </li>
                    <li>
		      <%= link_to t("show_representatives"), new_representative_path%>
                    </li>
                    <li>
                      <%= link_to t("show_find_us"), new_find_u_path%>
                    </li>
                </ul>
            </div>        
            <!--Menu End --> 
	</div>
